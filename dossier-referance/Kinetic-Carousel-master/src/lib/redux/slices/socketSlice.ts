// src/lib/redux/slices/socketSlice.ts
// THIS FILE IS OBSOLETE AND HAS BEEN REMOVED.
// All relevant state (isConnected, onlineUsers, etc.) is now managed
// within the `sessionSlice` to create a single source of truth for session-related data.
// The `useSocket.tsx` hook now dispatches actions to `sessionSlice` and `notificationSlice`.
