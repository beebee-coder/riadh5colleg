// This component is obsolete and will be removed.
// Its functionality has been moved to src/components/teacher/TeacherShortcuts.tsx
// to be displayed in context on the teacher's profile page.
'use client';

const Performance = ({ title }: { title: string }) => {
    return null; // The component is now empty and returns nothing.
};

export default Performance;
