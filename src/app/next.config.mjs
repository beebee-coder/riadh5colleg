/** @type {import('next').NextConfig} */
const nextConfig = {
    // La configuration des en-têtes a été supprimée ici et est maintenant gérée
    // par le fichier src/middleware.ts, qui est l'approche recommandée.
};

export default nextConfig;
