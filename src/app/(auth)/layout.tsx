// src/app/(auth)/layout.tsx
// This layout applies to all authentication-related pages.
export default function AuthPagesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
